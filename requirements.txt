altaipony @ git+https://github.com/gbryden/altaipony.git
astropy
astroquery
dawgie==2.1.0-rc15
deprecated
h5py
joblib
ldtk
lmfit
matplotlib
numexpr          # used to limit cores it uses preventing thrashing
numpy
pyOpenSSL==26.0.0  # 26.0.2 breaks the world so needs testing
photutils
pymc
pyvo
PyXB-CTC==1.3.3  # since code checks, just lock the version
service_identity # for twisted with TLS (optionally loaded so must do it here)
scipy
sympy
ultranest
xgboost          # for gemli machine learning inference
